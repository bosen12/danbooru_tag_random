# web4 · 導影台

Intent 左欄 → 成片中場 → 釘選台／詞庫 sheet。共用 `web/` 的 `boot.js`／`engine.js`（server `SHARED` 回退）。

啟動：`start-studio.bat` 或 `啟動-導影台.bat`（`WEB_DIR=web4`，埠 `8791`）。

主鍵「開拍」；中場只吃既有 `onStage`／取消；不預覽半套 POS。
