# 從 special_prompts 語料挖出來的新詞候選

來源：`C:\projects\special_prompts` 的 30792 個 `.py`（Grok 寫的情境詞庫）。

## 怎麼做的

1. 用 `ast` 解析每個檔的 `POSITIVE` 與 `REQUIRED_POSITIVE`（**不含 `NEGATIVE`**，那是要避開的東西），逗號切開、去掉 `(tag:1.2)` 這類權重語法 —— 30792 個檔全部解析成功，得到 **91926 個不重複字串**。
2. 扣掉我們已經有的：詞庫 1292 個 tag，再加上 `quality`／`nsfwTail`／`sfwTail`／`sensitiveTail` 和各種負向清單（`masterpiece`、`nsfw`、`explicit` 那些是尾巴加的，不是詞庫條目，不扣掉會誤報成「缺的」）。
3. 只取**用過 >= 5 個檔**的當候選 —— 只出現一兩次的幾乎都是散文片段，不是詞彙。剩 **8746 個**。
4. 全部打 Danbooru `tags.json` 查證。查得到但 **0 張圖**的再打 `tag_aliases.json`，看它指向哪個現役名字，並查那個名字的張數。

> 方法上踩過的一個坑：我原本以為「查不到的才可能是別名」，結果查出 0 個別名。實際上**別名也有 tag 紀錄**（只是 post_count 為 0），所以它們落在「查得到」那堆裡。改成對 0 張圖的那 1283 個查別名，才挖出 256 個現役別名。

### 結果

| | 數量 |
|---|---|
| 候選（用過 >=5 檔、我們沒有） | 8746 |
| 真的是 Danbooru tag 且有圖 | 833（其中 general 類 **802**） |
| 是現役別名，該用別的名字 | 256 |
| Danbooru 根本沒有（Grok 自己發明的） | 7905 |

**九成不能直接用**。這就是為什麼不能把語料倒進詞庫。

---

## 一、值得直接加（79 個）

篩選條件：**語料用過 >= 100 個檔**（是刻意的重複詞彙，不是偶發）且 **Danbooru >= 1000 張**（模型真的學過）且 category 為 general。數字是「語料檔數 / Danbooru 張數」。

### 場地與場景

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `desk` | 174 / 57218 | 我們有 office 但沒有桌子本身 |
| `bench` | 128 / 21943 | |
| `wooden bench` | 135 / 1291 | 公園、寺廟都用得上 |
| `nightstand` | 114 / 1097 | 床頭櫃，臥室場景的細節 |
| `cave` | 114 / 4143 | 奇幻／中世紀缺的場地 |
| `jungle` | 138 / 1467 | |
| `rural` | 146 / 1326 | 鄉間，和現有的 farm 互補 |
| `messy room` | 141 / 1878 | |
| `desk lamp` | 107 / 3931 | |
| `mop` | 135 / 4619 | 配 cleaning 這個活動 |

### 時節與場合

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `christmas` | 172 / 57892 | |
| `chinese new year` | 138 / 2375 | **古中國與現代都用得上**，正好補時代感 |
| `winter` | 124 / 18223 | |
| `wedding` | 130 / 5260 | |
| `racing` | 137 / 1000 | |

### 身分與職業

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `military` | 137 / 106871 | |
| `police` | 137 / 17214 | 我們有 policewoman，但沒有中性的 police |
| `pilot` | 169 / 1566 | |
| `delinquent` | 137 / 3192 | 不良少女／太保 |
| `mother and daughter` | 137 / 22300 | 多人組合，要配合卡司邏輯 |
| `racing suit` | 113 / 1217 | |
| `uniform` | 231 / 26589 | 泛用制服 |
| `badge` | 137 / 16363 | |
| `backpack` | 147 / 100787 | |

### 奇幻與非人

更正：我原本寫「幾乎整塊都沒有」也是錯的 —— 詞庫的 race 群已經有 37 個（goblin、orc、elf、dragon boy…）。真正缺的是下面這些**屬性與道具**，不是種族本身。

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `pointy ears` | 221 / **603678** | 精靈耳，張數極高、模型非常熟 |
| `mask` | 138 / 201193 | |
| `veil` | 139 / 98894 | |
| `mecha` | 162 / 55618 | |
| `android` | 341 / 32585 | |
| `ofuda` | 112 / 32944 | 符咒，配江戶／神社 |
| `green skin` | 152 / 31507 | |
| `magic` | 147 / 23486 | |
| `fantasy` | 838 / 20700 | 語料第二常用 |
| `superhero costume` | 110 / 20446 | |
| `giant` | 140 / 19070 | |
| `camouflage` | 102 / 15209 | |
| `tusks` | 167 / 6447 | |
| `surreal` | 155 / 6640 | |
| `tarot` | 137 / 4681 | |
| `prayer beads` | 134 / 3648 | 配古中國／江戶 |
| `tribal` | 380 / 2367 | |
| `giant male` | 114 / 1578 | |
| `medieval` | 192 / 1519 | 我們有 medieval **時代**，但沒有這個 tag 本身 |

### 服裝與暴露

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `lifting own clothes` | 261 / 88836 | 撩衣，很好用的暴露動作 |
| `large areolae` | 172 / 48785 | |
| `belly` | 185 / 23527 | |
| `wardrobe malfunction` | 281 / 11529 | 走光，正好配「暴露走光」那類情境 |

### 動作與互動

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `riding` | **909** / 23532 | 語料最常用的缺字。注意它是泛用的「騎」，不限體位 |
| `arms around neck` | 271 / 11897 | |
| `sandwiched` | 104 / 12756 | |
| `fighting` | 135 / 8584 | |
| `struggling` | 158 / 2646 | |
| `pinned` | 100 / 2650 | 我們有 pinned down，這是泛用版 |
| `bouncing` | 226 / 1071 | |

### 情緒與狀態

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `heart-shaped pupils` | 215 / 120862 | |
| `drunk` | 158 / 20284 | |
| `bruise` | 218 / 13406 | |
| `nervous smile` | 129 / 12110 | |
| `flustered` | 172 / 7352 | |
| `exhausted` | 285 / 3774 | |
| `messy` | 182 / 2066 | |

### 體液與性

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `cum overflow` | 145 / 41085 | |
| `internal cumshot` | 194 / 12590 | 中出，我們現在沒有 |
| `remote control vibrator` | 102 / 12359 | |
| `remote control` | 112 / 6695 | |
| `milk` | 255 / 7240 | |
| `interracial` | 398 / 35753 | |

### 環境

| tag | 檔數 / 張數 | 備註 |
|---|---|---|
| `wind` | 125 / 68104 | |
| `reflection` | 350 / 56567 | 鏡面反射，配我們已有的 mirror |
| `thermometer` | 115 / 1893 | |

---

## 二、第二梯（79 個，語料 25–99 檔且 >=1000 張）

沒有第一梯常用，但很多是**我們整個缺的類別**。我認為特別值得看的：

**綁縛與調教**（更正：我原本寫「完全沒有這一塊」是錯的 —— 我們有 `bondage`、`bdsm`、`restrained`、`handcuffs`、`leash`、`o-ring`、`collar`。缺的是下面這幾個具體道具）：`shibari` 46/24442、`bound wrists` 49/30119、`blindfold` 43/30386、`ball gag` 46/13654、`nipple clamps` 41/1929

**暴露細節**：`low neckline` 71/13410、`lace bra` 52/4901、`crotchless panties` 36/4287、`accidental exposure` 26/2726

**性**：`saliva trail` 61/28316、`deep penetration` 50/10097、`cum on tongue` 44/10309

**場地**：`tiles` 74/37218、`railing` 89/34321、`on desk` 31/10809、`stained glass` 47/6127、`sink` 29/5542、`counter` 35/4819、`under table` 32/4320、`toilet stall` 41/1854、`canopy bed` 55/1438、`dark room` 44/1283、`poker table` 31/1255、`village` 72/1246

**道具**：`katana` 80/83495、`camera` 83/32183、`christmas tree` 74/16024、`microphone stand` 31/9892、`steering wheel` 40/5106、`crystal ball` 77/2029、`shopping cart` 36/1628、`tea set` 55/1270

**互動**：`faceless male` 38/51902、`4boys` 25/47244、`kabedon` 75/5253

**自然**：`grass` 59/117602、`smoke` 39/58877、`full moon` 31/55948、`sand` 49/37683、`sun` 33/29980、`moss` 42/5363、`dust` 31/4160

---

## 三、要改名才能加（120 個）

這些 Grok 用的名字在 Danbooru 已經是**別名**，要用右邊的正規名。挑重要的：

| Grok 用的 | 正規名 | 張數 | 檔數 |
|---|---|---|---|
| `on sofa` | `on couch` | 35236 | 382 |
| `sci-fi` / `futuristic` | `science fiction` | 59599 | 346 / 327 |
| `wide eyes` | `wide-eyed` | 53504 | 222 |
| `injured` / `wounded` | `injury` | 40438 | 203 / 192 |
| `covering` | `covering privates` | 58686 | 138 |
| `water droplets` | `water drop` | 34759 | 136 |
| `palm trees` | `palm tree` | 32650 | 176 |
| `bookshelves` | `bookshelf` | 28166 | 177 |
| `superhero` | `superhero costume` | 20446 | 137 |
| `group` | `everyone` | 18563 | 230 |
| `legs around waist` | `leg lock` | 6342 | 170 |
| `peeping` | `peeking` | 4617 | 202 |
| `animal pelt` | `pelt` | 1537 | 243 |
| `countryside` | `rural` | 1326 | 136 |
| `relaxed` | `relaxing` | 1066 | 134 |
| `clothing store` | `clothes shop` | 409 | 139 |

### 一個陷阱

`cowgirl`（語料用了 878 個檔）在 Danbooru 會被導到 **`cow girl`（牛娘，14851 張）**，不是體位。Grok 顯然是要指騎乘位，那個字是 **`cowgirl position`** —— **而那個我們已經有了**。照著別名加會得到滿滿的牛耳少女。

---

## 四、我們已經有了，只是 Grok 用了別名（136 個）

不用加，但值得知道語料裡這些字其實在講我們已有的東西：

`doggy style`→`doggystyle`、`hot spring`→`onsen`、`mature woman`→`mature female`、`sofa`→`couch`、`titjob`→`paizuri`、`on knees`→`kneeling`、`cum on face`→`facial`、`wet skin`→`wet`、`breast grab`→`grabbing another's breast`、`soft smile`→`light smile`、`lockers`→`locker`、`cheating`→`cheating (relationship)`

**我們的詞庫本身是乾淨的** —— 這些的正規名我們都已經在用，沒有一個是用了過期名字。

---

## 五、Danbooru 沒有的（7905 個）—— 千萬別加

語料裡用得最兇的反而多半是這一類。前幾名：

| 字 | 檔數 |
|---|---|
| `adult woman` | 6741 |
| `overwhelmed` | 1217 |
| `eye-level framing` | 1128 |
| `close-up focus` | 1105 |
| `over-shoulder framing` | 981 |
| `slight low angle` | 842 |
| `taking turns` | 829 |
| `soft lamp` / `warm lamp` | 733 / 644 |
| `soft daylight` | 609 |
| `close intimate framing` | 592 |
| `cool blue night lighting` | 561 |
| `wide establishing framing` | 559 |

模式很清楚：**Grok 在寫攝影指導和散文**（框景、光質、情緒副詞），不是在寫 Danbooru tag。這些字模型沒學過，寫進 prompt 只會稀釋有效 token。

`adult woman` 特別值得注意 —— 6741 個檔在用，但 Danbooru 沒這個字，正解是我們已經有的 `mature female`。

---

## 六、要你決定的（非合意主題）

這幾個**機制上沒問題、Danbooru 也有**，但主題是非合意情境。我把它們單獨列出來讓你明確決定，而不是混在上面的清單裡讓它默默進來：

| tag | 檔數 / 張數 |
|---|---|
| `sexual harassment` | 623 / 735 |
| `humiliation` | 338 / 6264 |
| `bullying` | 321 / 1426 |
| `forced` | 221 / 899 |
| `objectification` | 196 / 2049 |
| `human furniture` | 194 / 1381 |
| `molestation` | 139 / 3726 |
| `public use` | 136 / 1614 |
| `abuse` | 80 / 1602 |

---

## 七、會牽動引擎的幾個

這幾個不是加進詞庫就好，會和現有邏輯打架，加之前要想清楚：

- **`solo focus`**（460 檔 / 504802 張）— 語意是「多人但只聚焦一人」，會和我們的 `solo` 與卡司數量邏輯直接衝突。張數極高，但這個要先想清楚再動。
- **`4boys`**（25 檔 / 47244 張）— 卡司表目前最多到 3boys，加了要同步改 `castWeights`。
- **`faceless male`**（38 檔 / 51902 張）— 會和「男性要交代身體」那條規則互動。
- **`mother and daughter`**（137 檔 / 22300 張）— 隱含兩人且有年齡差，要配合卡司。
- **`pinned`** — 我們已有 `pinned down`，兩個並存要確認互斥關係。

---

## 八、順手發現的一個 bug

查證過程中比對到的，跟這份清單無關但該記一下：

`web/engine.js:1304` 的 `SPORT_BAD_FURNITURE = new Set(["on bed", "bunk bed", "on sofa"])` 裡，**`"on sofa"` 不是詞庫裡的 tag**（`on bed` 和 `bunk bed` 都是）。Danbooru 的正規名是 `on couch`，而我們詞庫收的是 `couch`。

也就是說那條「運動器材不要掉到沙發上」的防護，對沙發從來沒有生效過。

---

## 附錄

802 個 general 真 tag 的完整資料（含張數）、256 個別名對照、7905 個不存在的字，都在查證結果的 JSON 裡。需要哪一段我可以再輸出成表。
